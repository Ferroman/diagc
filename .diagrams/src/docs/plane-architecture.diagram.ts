import { docsPlanes } from './planes.shared';

// Same model as docs/plane-infra and docs/plane-flow; only the plane order
// differs, so publish exports this one's architecture plane.
export default docsPlanes('docs-plane-architecture', 'Architecture plane', 'architecture');
